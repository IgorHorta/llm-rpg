# Copy this file to .env and fill in your values.
# For Fly.io: set secrets with `fly secrets set KEY=value` (do not commit .env).

# Gemini API (required for ADK agents)
GOOGLE_API_KEY=your_google_api_key_here

# Set to TRUE to use Vertex AI instead of Gemini API
GOOGLE_GENAI_USE_VERTEXAI=FALSE
